class BlankEffect {
  constructor(id, engine) {
    this.id = id;
    this.engine = engine;
  }

  addAffectObject(innerObject) {
  }

  render(ctx, visibleCallback) {
  }

  update(ctx, visibleCallback) {
  }

  flushAffectObject() {
  }
}
